# Claude Code entry point

Read `AGENTS.md` completely before working in this repository. It is the canonical shared project
context, product contract and audit protocol for both Claude Code and Codex.

Then follow the source-of-truth order defined there. Do not treat `TASKS.md`, old audits or checked
boxes as proof of current production state.

For an independent audit, remain read-only and create the dated audit document requested by
`AGENTS.md`. Do not fix, migrate, deploy, publish offers or send messages unless Boris asks for a
separate implementation pass.
