{
  "name": "offerpsp-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-offerpsp-portal.mjs",
    "build:admin": "npm --prefix platform-v2 ci && npm --prefix platform-v2 run build -- --base=/admin/",
    "build": "npm run validate && node scripts/build-vercel-output.mjs"
  }
}
